<section class="hero">
	<div class="glow"></div>

	<div class="content">
		<div class="logo-container">
			<img
				src="/logo.png" alt="SpectroDinner Logo" class="logo"
			/>
		</div>

		<h1>
			SpectroDinner™
		</h1>

		<div class="free-badge">
			<span data-lang="es">✨ Servicio 100% gratuito</span>
			<span data-lang="en">✨ 100% free service</span>
		</div>

		<h2>
			<span data-lang="es">Creamos, configuramos y rediseñamos tu servidor de Discord</span>
			<span data-lang="en">We create, configure and redesign your Discord server</span>
		</h2>

		<p class="description" data-lang="es">
			Diseñamos servidores de Discord a la medida de cada cliente: desde cero, mejorando uno ya existente o rediseñándolo por completo, sin ningún coste. Todo empieza con un cuestionario donde nos cuentas el diseño, la temática y para qué quieres tu servidor, para entregarte exactamente lo que imaginas.
		</p>

		<p class="description" data-lang="en">
			We design Discord servers tailored to each client: from scratch, improving an existing one, or redesigning it completely, at no cost. It all starts with a questionnaire where you tell us the design, theme and purpose of your server, so we deliver exactly what you imagine.
		</p>

		<div class="buttons">
			<a
				href="/cuestionario"
				class="primary"
			>
				<span data-lang="es">Rellenar el cuestionario →</span>
				<span data-lang="en">Fill out the questionnaire →</span>
			</a>

			<a
				href="#servicios"
				class="secondary"
			>
				<span data-lang="es">Ver servicios</span>
				<span data-lang="en">View services</span>
			</a>
		</div>

		<div class="hero-tags">
			<span data-lang="es">CREACIÓN • CONFIGURACIÓN • REDISEÑO • 100% GRATIS</span>
			<span data-lang="en">CREATION • CONFIGURATION • REDESIGN • 100% FREE</span>
		</div>
	</div>
</section>

<style>

.hero {
	min-height: 85vh;
	width: 100%;

	display: flex;
	align-items: center;
	justify-content: center;

	position: relative;
	overflow: hidden;

	padding: 80px 32px;
}

.glow {
	position: absolute;

	width: 650px;
	height: 650px;

	background:
		radial-gradient(
			circle,
			rgba(10,132,255,.15),
			transparent 70%
		);

	filter: blur(70px);

	z-index: -1;

	animation: glowFloat 8s ease-in-out infinite;
}

@keyframes glowFloat {
	0%, 100% {
		transform: translate(0, 0) scale(1);
	}

	50% {
		transform: translate(0, -20px) scale(1.06);
	}
}

.content {
	width: 100%;
	max-width: 900px;

	display: flex;
	flex-direction: column;
	align-items: center;

	text-align: center;
}

.content > * {
	animation: heroFadeUp .8s cubic-bezier(.16,1,.3,1) both;
}

.content > *:nth-child(1) { animation-delay: .05s; }
.content > *:nth-child(2) { animation-delay: .18s; }
.content > *:nth-child(3) { animation-delay: .30s; }
.content > *:nth-child(4) { animation-delay: .42s; }
.content > *:nth-child(5) { animation-delay: .52s; }
.content > *:nth-child(6) { animation-delay: .52s; }
.content > *:nth-child(7) { animation-delay: .64s; }
.content > *:nth-child(8) { animation-delay: .76s; }

@keyframes heroFadeUp {
	from {
		opacity: 0;
		transform: translateY(26px);
	}

	to {
		opacity: 1;
		transform: none;
	}
}

.logo-container {
	width: 120px;
	height: 120px;

	margin-bottom: 45px;

	display: flex;
	align-items: center;
	justify-content: center;

	perspective: 1000px;
}

.logo {
	width: 100%;
	height: 100%;

	object-fit: contain;

	filter:
		drop-shadow(
			0 0 20px
			rgba(10,132,255,.35)
		);

	transform-style: preserve-3d;

	animation:
		logoSpin 18s linear infinite;
}

@keyframes logoSpin {
	from {
		transform: rotateY(0deg);
	}

	to {
		transform: rotateY(360deg);
	}
}

h1 {
	margin: 0;

	font-size: clamp(3rem,7vw,5.5rem);
	font-weight: 800;

	letter-spacing: -3px;

	color: white;
}

.free-badge {
	margin-top: 22px;

	display: inline-flex;
	align-items: center;
	justify-content: center;

	padding: 10px 20px;

	border-radius: 999px;

	background: rgba(10,132,255,.12);

	border: 1px solid rgba(10,132,255,.35);

	color: #0A84FF;

	font-size: .85rem;
	font-weight: 700;

	letter-spacing: .5px;
}

h2 {
	margin: 20px 0 0;

	font-size: clamp(1.8rem,4vw,3rem);
	font-weight: 500;

	color: #9CA3AF;

	letter-spacing: -1px;
}

.description {
	margin-top: 32px;

	max-width: 760px;

	font-size: 1.15rem;
	line-height: 1.8;

	color: #B6BDC9;

	letter-spacing: -.2px;
}

.buttons {
	display: flex;

	gap: 20px;

	margin-top: 50px;
}

.buttons a {
	padding: 15px 32px;

	border-radius: 14px;

	text-decoration: none;

	font-weight: 600;

	transition: .25s ease;
}

.primary {
	background: #0A84FF;
	color: white;
}

.primary:hover {
	transform: translateY(-3px);

	box-shadow:
		0 15px 40px
		rgba(10,132,255,.35);
}

.secondary {
	background:
		rgba(255,255,255,.05);

	border:
		1px solid
		rgba(255,255,255,.12);

	color: white;
}

.secondary:hover {
	background:
		rgba(255,255,255,.1);

	transform: translateY(-3px);
}

.hero-tags {
	margin-top: 48px;

	font-size: .9rem;
	font-weight: 600;

	letter-spacing: 2px;
	text-transform: uppercase;

	color: #7C8593;
}

@media (max-width: 768px) {

	.hero {
		padding: 60px 24px;
	}

	.logo-container {
		width: 90px;
		height: 90px;
	}

	.description {
		font-size: 1rem;
		line-height: 1.7;
	}

	.buttons {
		flex-direction: column;
		width: 100%;
	}

	.buttons a {
		width: 100%;
	}

	.hero-tags {
		font-size: .8rem;
		line-height: 1.8;
	}
}

</style>