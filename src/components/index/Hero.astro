<section class="hero">
	<div class="glow"></div>

	<div class="content">
		<div class="logo-container">
			<img
				src="/logo.png" alt="SpectroDinner Logo" class="logo"
			/>
		</div>

		<h1>
			SpectroDinner™
		</h1>

		<div class="badge">
			<span data-lang="es">ESTUDIO DE DESARROLLO INDEPENDIENTE</span>
			<span data-lang="en">INDEPENDENT DEVELOPMENT STUDIO</span>
		</div>

		<h2>
			<span data-lang="es">Software · Juegos · Minecraft · Tecnología Experimental</span>
			<span data-lang="en">Software · Games · Minecraft · Experimental Technology</span>
		</h2>

		<p class="description" data-lang="es">
			SpectroDinner Studios diseña y construye software, videojuegos
			y proyectos de Minecraft, además de explorar tecnología
			experimental. También ofrecemos un servicio gratuito de
			configuración de servidores de Discord.
		</p>

		<p class="description" data-lang="en">
			SpectroDinner Studios designs and builds software, games and
			Minecraft projects, while also exploring experimental
			technology. We also offer a free Discord server setup
			service.
		</p>

		<div class="buttons">
			<a
				href="/projects"
				class="primary"
			>
				<span data-lang="es">Explorar Proyectos →</span>
				<span data-lang="en">Explore Projects →</span>
			</a>

			<a
				href="#servicios"
				class="secondary"
			>
				<span data-lang="es">Trabaja con Nosotros</span>
				<span data-lang="en">Work With Us</span>
			</a>
		</div>
	</div>
</section>

<style>

.hero {
	min-height: 85vh;
	width: 100%;

	display: flex;
	align-items: center;
	justify-content: center;

	position: relative;
	overflow: hidden;

	padding: 80px 32px;
}

.glow {
	position: absolute;

	width: 650px;
	height: 650px;

	background:
		radial-gradient(
			circle,
			color-mix(in srgb, var(--primary) 15%, transparent),
			transparent 70%
		);

	filter: blur(70px);

	z-index: -1;

	animation: glowFloat 8s ease-in-out infinite;
}

@keyframes glowFloat {
	0%, 100% {
		transform: translate(0, 0) scale(1);
	}

	50% {
		transform: translate(0, -20px) scale(1.06);
	}
}

.content {
	width: 100%;
	max-width: 900px;

	display: flex;
	flex-direction: column;
	align-items: center;

	text-align: center;
}

.content > * {
	animation: heroFadeUp .8s cubic-bezier(.16,1,.3,1) both;
}

.content > *:nth-child(1) { animation-delay: .05s; }
.content > *:nth-child(2) { animation-delay: .18s; }
.content > *:nth-child(3) { animation-delay: .30s; }
.content > *:nth-child(4) { animation-delay: .42s; }
.content > *:nth-child(5) { animation-delay: .52s; }
.content > *:nth-child(6) { animation-delay: .64s; }

@keyframes heroFadeUp {
	from {
		opacity: 0;
		transform: translateY(26px);
	}

	to {
		opacity: 1;
		transform: none;
	}
}

.logo-container {
	width: 120px;
	height: 120px;

	margin-bottom: 45px;

	display: flex;
	align-items: center;
	justify-content: center;

	perspective: 1000px;
}

.logo {
	width: 100%;
	height: 100%;

	object-fit: contain;

	filter:
		drop-shadow(
			0 0 20px
			var(--primary-glow)
		);

	transform-style: preserve-3d;

	animation:
		logoSpin 18s linear infinite;
}

@keyframes logoSpin {
	from {
		transform: rotateY(0deg);
	}

	to {
		transform: rotateY(360deg);
	}
}

h1 {
	margin: 0;

	font-size: clamp(3rem,7vw,5.5rem);
	font-weight: 800;

	letter-spacing: -3px;

	color: var(--text);
}

.badge {
	margin-top: 22px;

	display: inline-flex;
	align-items: center;
	justify-content: center;

	padding: 10px 20px;

	border-radius: 999px;

	background: var(--primary-bg);

	border: 1px solid var(--primary-border);

	color: var(--primary);

	font-size: .8rem;
	font-weight: 700;

	letter-spacing: 2px;
	text-align: center;
}

h2 {
	margin: 24px 0 0;

	font-size: clamp(1.4rem,3.4vw,2.2rem);
	font-weight: 500;

	color: var(--text-muted);

	letter-spacing: -.5px;
}

.description {
	margin-top: 28px;

	max-width: 700px;

	font-size: 1.1rem;
	line-height: 1.8;

	color: var(--text-muted);

	letter-spacing: -.2px;
}

.buttons {
	display: flex;

	gap: 20px;

	margin-top: 46px;
}

.buttons a {
	padding: 15px 32px;

	border-radius: 14px;

	text-decoration: none;

	font-weight: 600;

	transition: .25s ease;
}

.primary {
	background: var(--primary);
	color: white;
}

.primary:hover {
	transform: translateY(-3px);

	box-shadow:
		0 15px 40px
		var(--primary-glow);
}

.secondary {
	background:
		var(--surface-2);

	border:
		1px solid
		var(--border-2);

	color: var(--text);
}

.secondary:hover {
	background:
		var(--surface-hover);

	transform: translateY(-3px);
}

@media (max-width: 768px) {

	.hero {
		padding: 60px 24px;
	}

	.logo-container {
		width: 90px;
		height: 90px;
	}

	.description {
		font-size: 1rem;
		line-height: 1.7;
	}

	.buttons {
		flex-direction: column;
		width: 100%;
	}

	.buttons a {
		width: 100%;
	}
}

</style>
