---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
---

<Layout title="Documentation | SpectroDinner™">

	<Navbar />

	<main class="container">
		<section class="hero">
			<h1>Documentation</h1>

			<p>
				Technical documentation, guides and resources
				for SpectroDinner™ projects.
			</p>
		</section>

		<section class="cards">

			<div class="card reveal spotlight">
				<h2>Getting Started</h2>
				<p>
					Learn how to install, configure and use
					our projects.
				</p>
			</div>

			<div class="card reveal spotlight">
				<h2>API Reference</h2>
				<p>
					Technical references, integrations and
					developer resources.
				</p>
			</div>

			<div class="card reveal spotlight">
				<h2>Guides</h2>
				<p>
					Step-by-step tutorials and detailed
					explanations.
				</p>
			</div>

		</section>
	</main>

	<Footer />

</Layout>

<style>
.container {
	max-width: 1200px;
	margin: 100px auto;
	padding: 0 32px;
}

.hero {
	text-align: center;
	margin-bottom: 70px;
}

h1 {
	font-size: 4rem;
	margin-bottom: 20px;

	background: linear-gradient(
		90deg,
		var(--text),
		var(--primary)
	);

	-webkit-background-clip: text;
	-webkit-text-fill-color: transparent;
}

.hero p {
	color: var(--text-muted);
	font-size: 1.2rem;
}

.cards {
	display: grid;
	grid-template-columns: repeat(
		auto-fit,
		minmax(280px, 1fr)
	);

	gap: 25px;
}

.cards .card:nth-child(2) { transition-delay: .08s; }
.cards .card:nth-child(3) { transition-delay: .16s; }

.card {
	padding: 30px;

	background: var(--surface-2);

	border: 1px solid var(--border-1);
	border-radius: 20px;

	backdrop-filter: blur(15px);

	transition: .25s ease;
}

.card:hover {
	transform: translateY(-5px);
	border-color: color-mix(in srgb, var(--primary) 50%, transparent);
}

.card h2 {
	margin-top:0;
	color:var(--text);
}

.card p {
	color:var(--text-muted);
	line-height:1.6;
}
</style>